#include "sort.h"
/**
 * bitonic_sort - bitonic
 * desc: using the Bitonic sort algorithm
 * @array: iarray
 * @size: size
 */
void bitonic_sort(int *array, size_t size)
{
	(void)array;
	(void)size;
}
